import prototypes = require('./../dev-utils/prototypes');
import tokens = require('./../dev-utils/tokens');

module Utilities {
    export var Prototypes = prototypes;
    export var Tokens = tokens;
}

export = Utilities;
